# Shared musubi-tuner Krea 2 runtime
#
# The launcher installs vendor/sd-scripts/requirements.txt first, then this
# application-owned file last. That makes this version set authoritative for
# both sd-scripts and musubi without modifying either upstream repository.
#
# Do not add torch or torchvision here: the main environment's CUDA 13 build
# is installed before either upstream requirement set and is shared by every
# training core. tools.ensure_musubi_runtime prevents repeated pip work once
# these versions are already present.

accelerate==1.6.0
av==14.0.1
bitsandbytes
diffusers==0.32.1
einops==0.7.0
huggingface-hub==0.34.3
opencv-python==4.10.0.84
pillow>=11.3.0
prodigy-plus-schedule-free==1.9.2
safetensors==0.4.5
toml==0.10.2
tqdm==4.67.1
transformers==4.57.6
tokenizers==0.22.2
voluptuous==0.15.2
ftfy==6.3.1
easydict==1.13
sentencepiece==0.2.1
