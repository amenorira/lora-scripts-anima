# ============================================================
# 项目额外依赖
# sd-scripts 核心依赖由 vendor/sd-scripts/requirements.txt 安装，
# 此处仅列出项目 GUI / 后端 / 扩展所需的额外包。
# 安装顺序：先 sd-scripts，再本文件。
# ============================================================

# ── 后端（FastAPI）──
fastapi==0.103.2
pydantic>=2.0,<3
uvicorn==0.22.0
httpx==0.24.1
python-multipart==0.0.18
websockets==14.2

# ── 系统监控 ──
psutil>=7.0.0
nvidia-ml-py==13.595.45

# ── 数据处理 ──
pandas>=2.0
pillow>=10.0

# ── 构建工具（TensorBoard 依赖 pkg_resources，setuptools>=82 已移除该模块）──
setuptools>=68,<82

# ── 训练扩展（vendor/sd-scripts 代码需要但 vendor/requirements.txt 未声明）──
lycoris-lora==3.4.0
